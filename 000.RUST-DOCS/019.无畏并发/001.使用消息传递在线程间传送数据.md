# 使用消息传递（message passing）在线程间传送数据
|摘要|说明|备注|
|-|-|-|
|- 这个思想来源于 Go 编程语言文档 中的口号：“不要通过共享内存来通讯；而要通过通讯来共享内存。”（“Do not communicate by sharing memory; instead, share memory by communicating.”）|-|-|
|-|-|-|
|- 为了实现消息传递并发，Rust 标准库提供了一个信道（channel）实现|- 数据从一个线程传递到另一个线程|-|
|-|-|-|
|- std::sync::mpsc|- mpsc 是 多生产者，单消费者（multiple producer, single consumer）的缩写|-|
|-|-|-|
|-|-|-|
|-|-|-|
|-|-|-|
|-|-|-|
|-|-|-|

---

## 线程间通信示例
```rust
use std::sync::mpsc;
use std::thread;

fn main() {
    // mpsc::channel 函数返回一个元组：第一个元素是发送侧 – 发送端，而第二个元素是接收侧 – 接收端
    let (tx, rx) = mpsc::channel();

    thread::spawn(move || {
        let val = String::from("hi");
        // send 函数获取其参数的所有权并移动这个值归接收端所有
        tx.send(val).unwrap();
    });

    let received = rx.recv().unwrap();
    println!("Got: {received}");
}
```

---

## 信道与所有权转移
|注意事项|说明|备注|
|-|-|-|
|- send 函数获取其参数的所有权并移动这个值归接收端所有|-|- 发送之后，就不能再用这个值了|
|-|-|-|
|-|-|-|


---

## 通过克隆发送端来创建多个生产者
```rust
use std::sync::mpsc;
use std::thread;
use std::time::Duration;

fn main() {
    let (tx, rx) = mpsc::channel();

    // 创建一个新的生产者
    let tx1 = tx.clone();
    thread::spawn(move || {
        let vals = vec![
            String::from("hi")
        ];

        for val in vals {
            tx1.send(val).unwrap();
            thread::sleep(Duration::from_secs(1));
        }
    });

    thread::spawn(move || {
        let vals = vec![
            String::from("more")
        ];

        for val in vals {
            tx.send(val).unwrap();
            thread::sleep(Duration::from_secs(1));
        }
    });

    for received in rx {
        println!("Got: {received}");
    }
}

```
